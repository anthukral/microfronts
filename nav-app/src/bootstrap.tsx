import React from "react";
import ReactDOM from "react-dom";
import Nav from "./Nav";
const App = () => <Nav />;

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
